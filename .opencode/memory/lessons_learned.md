# Lessons Learned
This file contains the dynamic memory of the agent. The QA Agent updates this file.
When you make a mistake, encounter a bug, or fix a tricky issue, document it here so future agents do not repeat it.

## Gotchas and Quirks
- [Date] - (Example: Do not use `npm i` without `--legacy-peer-deps` on this project because of dependency conflicts).
